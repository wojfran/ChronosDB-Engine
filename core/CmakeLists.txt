add_library(chronosdb_core STATIC 
    src/DatabaseCore.cpp
    src/IndexProvider.cpp
    src/BinaryStorageManager.cpp
    src/JsonStorageManager.cpp
    src/BenchmarkEngine.cpp
)

target_include_directories(chronosdb_core 
    PUBLIC 
        include
)

target_link_libraries(chronosdb_core 
    PUBLIC 
        common
        sqlite3
)